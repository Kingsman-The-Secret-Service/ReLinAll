from relinall.ui import *
from relinall.db import *
from relinall.os import *

if __name__ == "__main__":
       
	MainWindow()
	# s= server()
	# s.getServerData()

	# app = QApplication(sys.argv)
	# window = Window()
	# window.show()
	# sys.exit(app.exec_())